{{ set(this, 'title', 'About') }}
{{ set(this, 'params', { 'breadcrumbs' : { '' : this.title } }) }}

<div class="main">
    <div class="section">
        <div class="container">
            <div class="row">
                <div class="col-md-8 ml-auto mr-auto">
                    <h2 class="title">{{ this.title }}</h2>
                    <h5 class="description">This is the About page. You may modify the following file to customize its content:</h5>
                    <br>
                    <code>{{ this.viewFile }}</code>
                </div>
            </div>
        </div>
    </div>
</div>